import { type } from "os";
import { ReactNode } from "react";
import { PageContainer } from "./style";
type DefaultPageProps = {
  children?: ReactNode
}
export function DefaultPage(prop: DefaultPageProps){
return (
  <PageContainer>
    
  </PageContainer>
)
}